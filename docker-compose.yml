version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    # Optional: Mount your source code to enable live reloading during development
    # volumes:
    #   - .:/usr/src/app
    #   - /usr/src/app/node_modules
